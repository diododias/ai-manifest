# Work Items

A file represents a unit of work and is the source of truth for its state.

- [`WI-031.md`](WI-031.md): active example linked to `PLAN-014`.

Allowed states: `backlog`, `refinement`, `ready`, `planning`, `implementation`, `review`, `validation`, `blocked`, `done` and `cancelled`.
