interface:
  display_name: "Workspace Memory"
  short_description: "Return to context without treating memory as a source of truth."
  default_prompt: "Use $workspace-memory to safely resume a mission."
